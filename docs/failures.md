# Dziennik porażek — co nie zagrało i dlaczego

Cel: kolejne iteracje nie powtarzają ślepych uliczek. Wpisy chronologicznie, najnowsze na dole.

## Z V5 (zebrane wstecznie)

- **`gsap.quickTo(el, "scale")` + resetTo** → warning „scale not eligible for reset" przy każdym hover-klik cyklu kursora. GSAP każe rozbijać na osie: quickTo scaleX + scaleY za fasadą. Ta sama klasa: tweeny ze `scale` w kontekstach `revertOnUpdate` śmiecą konsolę przy przełączaniu scenariuszy — spring na samym `y` czyta się tak samo.
- **Lighthouse przy dev-serverze w tle kłamie**: TBT 460 ms vs 60 ms na czystej maszynie (kontencja CPU). Mierzyć: kill dev → build → serve out/ → LH; bisekcja przez `git worktree` + symlink node_modules.
- **Lantern (symulacja) LCP ≠ rzeczywistość**: 9,1 s symulowane vs 180 ms obserwowane (LCP = akapit hero). Dowód zawsze przez `--throttling-method=devtools` + PerformanceObserver w realnym Chromium.
- **Pexels/Unsplash blokują curl na stronach HTML** (Cloudflare/anty-bot) — działa WebFetch; CDN images.pexels.com przez curl OK, ALE nowsze zdjęcia mają NIESTANDARDOWE nazwy plików (brać z og:image, nie zgadywać wzorca).
- **Packshoty butów/kasków bez logo praktycznie nie istnieją w darmowych stockach** — kilkanaście odrzutów (HOKA/POC/SMITH/Merrell/…); strategia: kadr omijający logo albo produkt premium bez brandingu.
- **drei `<View track>` poza Canvasem** ignoruje track (mierzy własny pusty div) — Views MUSZĄ być dziećmi Canvasa.
- **`*/` wewnątrz komentarza CSS** zamyka komentarz (zmielone tokeny) — komentarze tokenów bez sekwencji `*/`.

## V6

- **2026-07-10 · Gradient jako TŁO bąbla IG (V5) nie przetrwał anti-kitsch** — „rozpoznawalność kanału" próbowaliśmy zrobić kolorem, a robi ją GEOMETRIA (pigułki, reply-quote, „Aktywna teraz"). Kolor w cudzym wzorcu czyta się jak cukierek. Wniosek: sygnały kanału = layout; kolor zawsze w naszej palecie, wyciszony; gradient maks. jako 1px ring.
- **2026-07-10 · quickTo z oklch w asercji kontrastu** — parser „pierwsze 3 liczby z getComputedStyle" łamie się na `oklch(...)` (Chrome zwraca oklch dla color-mix in oklch). Mierzyć przez canvas fillStyle→getImageData (przeglądarka konwertuje sama).
- **2026-07-10 · Foto-gate oblał plecak i elektronikę** — nie przez brak fotografii, tylko przez SŁABE KADRY z V5: produkt mały na jednolitym tle (thumb 112 px tracił całą fakturę — entropia 2.15, wizualnie „ikona"). To wyjaśnia część piksel-audytu klienta. Fix: ciasny kadr u źródła (pipeline), NIE osłabianie progu; jedynie próg mono dla thumbów skalibrowany 5.0→4.6 (downsampling zjada ~1.5 pkt entropii; margines nad noisy-tile ~1.2 pkt zachowany).
- **2026-07-10 · Treść kroków przywiązana do scrubu = pusta karta NA SNAPIE** — w Trzech krokach `animatePanelContent` startował na 0.25 osi scrubu, więc etykieta spoczynku s0 (do której snap CELOWO ściąga) pokazywała pustą ramkę, a chip „podpięto" (at+1.6) wypadał już po wyjeździe panelu. Ta sama klasa co „36 031 mid-scrub". Wniosek: STAN SPOCZYNKU (snap label) musi być kompletny — treść pierwszego stanu gra time-based przy wejściu w pin, okna kolejnych domykają się PRZED etykietą.
- **2026-07-10 · Elipsa converge wylądowała na microcopy finału** — pas GL centrowany „na oko" (`top-[22%]`) położył zbieg cząstek dokładnie na linii „Odpowiadamy…" (dwa niezależne screeny). Fix: pas niżej (`top-[34%]`), elipsa w pustej strefie dołu = „lądowisko" pod formularzem; werdykt z geometrii (microBottom 661 < glCenter 729) + screen.
- **2026-07-10 · Ocena koloru z przeskalowanego PNG myli** — kropka 02 postępu wyglądała na nieaktywną przy aktywnej 03 (złamana monotonia = podejrzenie buga). Dump `data-active` z DOM pokazał [t,t,t] na każdej głębokości — artefakt kompresji/skalowania. Wniosek: subtelne kolory weryfikować stanem DOM, nie pikselami screenshota.
- **2026-07-10 · Playwright z ESM nie rozwiązuje się względem cwd** — skrypt w scratchpadzie poza repo nie widzi `node_modules` niezależnie od katalogu uruchomienia (resolution idzie od ŚCIEŻKI PLIKU). Fix: absolutny import `<repo>/node_modules/playwright/index.mjs`.
- **2026-07-11 · Sonda „opacity>0.1" nie wykrywa display:none** — tryb static chowa typing-dots przez `display:"none"`, a computed opacity zostaje 1 → asercja B5 fałszywie krzyczała „replay". Widoczność mierzyć parą: `offsetParent !== null` + opacity; „widoczny tekst" najprościej przez `innerText` (pomija visibility:hidden z autoAlpha i display:none).
- **2026-07-11 · Scroll „do sekcji" ≠ scroll do elementu z triggerem** — jump do góry #kanaly zostawiał switcher (sekcja + ~800 px) pod linią startu ST: playback nigdy nie ruszał i trzy asercje padały kaskadą. W testach celować w ELEMENT niosący trigger, nie w kotwicę sekcji.
- **2026-07-11 · Cztery sondy = cztery evaluate = dziurawe próbkowanie** — okno czasowe 150–350 ms bywało bez ani jednego sampla, bo każdy tick robił 4 round-tripy. Sondy wielopolowe zawsze JEDNYM evaluate.

## V7 (zespół agentów QA — F0)

- **2026-07-11 · Mobilny `mouse.wheel` scrolluje wewnętrzny log czatu, nie stronę** — kursor nad kontenerem z `data-lenis-prevent` + `overflow-y-auto` łyka wheel → strona nigdy nie dojeżdża do dna → timeout przejazdu. Desktop działał (kursor w lewym marginesie nad stroną). Fix: mobile <768 skrolluje programowo `window.scrollTo` (Lenis się synchronizuje; mobile i tak bez pinów). Desktop zostaje na wheel (testuje scrub/piny przez Lenis).
- **2026-07-11 · Adwersarialny review suity znalazł 5 asercji-widm** (workflow 19 agentów) — których żywe przebiegi NIE łapią, bo „vacuous pass wygląda jak realny pass": (a) `querySelector(".pp-results")` na mobile czytał UKRYTY desktopowy węzeł (pierwszy w DOM, rect 0×0) → searchEmpty zawsze false → asercja pusta; fix `find(offsetParent!==null)`; (b) kłódka tonu to `role="switch"`/`aria-checked`, a test czytał nieistniejące `aria-pressed`/`data-on` → `""===""` zawsze prawda; (c) kotwica „ten sam mózg" brała zdanie USERA (trywialnie w każdym skinie + w email jako subject), nie BOTA; (d) `dwell()` wykluczał stan „0" globalnie, a dla probIdx/pillarIdx 0 = pierwszy stat/filar → pierwszy stan nigdy nie sprawdzany. Wniosek: żywy zielony ≠ sensowny test; adwersarialny review testów PRZED zaufaniem bramce.
- **2026-07-11 · Visual-regression na dynamicznej stronie — trzy źródła niedeterminizmu** naraz: (1) rdzeń cząstek GL DRYFUJE między przebiegami (halo diff wokół czatu) → maskować `canvas` (visibility:hidden); (2) tall-element screenshot STITCHUJE podczas gdy layout wciąż osiada (kaskada `setDone`→quickReplies→`setSeen` zmienia wysokość PO paincie) → kumulatywny dryf pionowy → rozgrzewający przejazd + czekanie na STABILNĄ wysokość; (3) subpikselowe zaokrąglenie wysokości sekcji ±1 px → „100% diff" (niezgodność wymiarów) → tolerancja ±2 px z cropem do wspólnego prostokąta. Dopiero wszystkie trzy razem dają zielony przy progu 0,5%.
- **2026-07-11 · `reducedMotion` w projekcie `use` bywa nieaplikowany** — cząstki renderowały się w visual mimo `use.reducedMotion:"reduce"` (GLStage line 15 respektuje reduce, więc canvas był=false → reduce nie działał). Probe potwierdził: `browser.newContext({reducedMotion})` ORAZ `page.emulateMedia({reducedMotion})` dają canvas=0. Fix: jawne `page.emulateMedia({reducedMotion:"reduce"})` w teście (autorytatywne runtime), nie polegać na config.
- **2026-07-11 · Lighthouse: chrome-launcher + Playwright chromium WISI** przy starcie (zostawia osierocony serwer). Fix: chrome-launcher auto-wykrywa SYSTEMOWY Google Chrome (standardowa ścieżka LH) — bez `chromePath`. Playwright chromium tylko w fallbacku LCP (własne `chromium.launch()` działa).
- **2026-07-11 · Lighthouse pod obciążeniem tła KŁAMIE** (potwierdzenie V6) — interaktywny Chrome usera przy 57% CPU zaniżył desktop Perf 94→50, TBT 60→313 ms. Nie kod, lecz środowisko. Fix: qa-lighthouse ŚWIADOMY KONTENCJI — gdy desktop TBT >150 ms (proxy „nie-idle"), perf NIEROZSTRZYGAJĄCY (report, nie FAIL), a11y (niewrażliwe na CPU) zawsze twardo. Perf bramkowany dopiero na idle. Mobile Perf <80 = known-issue do F3 (`LH_MOBILE_STRICT=1` czyni twardym).
- **2026-07-11 · macOS nie ma `timeout`** (coreutils) — `timeout 420 npx…` → „command not found", komenda nie rusza. Playwright ma własny per-test timeout, polegać na nim.
- **2026-07-11 · `test.fail()` jako kontrakt między fazami** — asercje na rzeczy naprawiane później (F1/F2/F3) wchodzą z markerem `test.fail()`: suita zielona, a gdy naprawa zadziała, test „passes unexpectedly" → marker sam krzyczy do zdjęcia. UWAGA kalibracja: wolny przejazd counterów DZIŚ przechodzi (ST zdąża), więc marker tylko na szybkim/urwanym wariancie — inaczej fałszywy „passed unexpectedly".

## V7 (chat engine — F1)

- **2026-07-11 · Guard CZASOWY (programmaticUntil) połyka scroll usera podczas playbacku** — żeby odróżnić scroll programowy od userowego użyłem okna 600 ms po każdym stick. Ale sticki są CZĘSTE (co wiadomość/tik), więc guard prawie zawsze aktywny → wheel-up usera ignorowany → pinned nigdy false → pigułka nie wstaje. Fix: NIE czas, lecz `expectedTop` — zapamiętaj scrollTop który USTAWIŁEŚ; w słuchaczu `|scrollTop − expectedTop| ≤ 2` = nasz scroll (ignoruj), inaczej user. Wymaga sticków INSTANT (smooth przechodzi przez wartości pośrednie ≠ expectedTop → mylone z userem).
- **2026-07-11 · expectedTop musi być FAKTYCZNY scrollTop po klampie** — `scrollTo({top})` klampuje do maxScroll (scrollHeight−clientHeight). Gdy target>max (kotwica poniżej treści, PAD, msg jeszcze nie w scrollHeight), scrollTop ląduje na max ≠ target → słuchacz myli nasz scroll z userowym → fałszywe pinned=false → sticki gasną → wiadomości uciekają z widoku (ratio 0.12 late w rozmowie). Fix: `expectedTop = body.scrollTop` ODCZYTANE po scrollTo (instant = synchroniczne, więc już po klampie).
- **2026-07-11 · Stick PO reveal-tweenie = ~0,45 s wiadomości poza widokiem** — `.call(stick)` po springu msg (0.45 s) / reveal badge zostawiał okno, w którym nowy krok był już widoczny (opacity 1, typing zgasł), ale log jeszcze nie przewinięty → ratio 0.12–0.24, `typingVisible:false` = zgłoszony „pisanie poza ekranem". Fix: stick PRZED reveal — po kolapsie dots (`display:none`) layout msg jest FINALNY (autoAlpha:0 rezerwuje wysokość), więc scroll natychmiast, a spring jest tylko wizualny (transform y + opacity) w już-przewiniętym miejscu. Dowód: sonda 8–14 s (scrollTop/ratio/pinned per 500 ms) pokazała lag mount→stick, nie błąd pinned.
- **2026-07-11 · Pre-layout czatu (`.chat-step{opacity:0}`) łamie formułę briefu `scrollHeight−scrollTop−clientHeight<24`** — scrollHeight PEŁNY od 1. klatki (wszystkie kroki w DOM), więc „przy dole" wg tej formuły jest ZAWSZE fałszem na starcie. Pinned liczony względem KOTWICY (dołu aktywnego kroku), nie scrollHeight — zachowuje semantykę „user przy żywej krawędzi rozmowy". [[hackmysales-v3-rebuild]]

## V7 (mobile — F2/F3/F4)

- **2026-07-11 · `Counter` z `immediateRender:true` NISZCZY SSR na mount** — `gsap.fromTo(obj,{v:0},…)` przy mount od razu maluje klatkę 0, kasując poprawne SSR „98" do „0"; powrót zależny od once-ST na inline `text-[24vw]` (refresh odroczony do fonts.ready+rIC) → na wolnym mobile ST nie zdąża → „samo %". Fix (Counter v4): DOM ZAWSZE pokazuje wartość finalną (SSR nietknięty), dip 0.7×→1× odpalany IntersectionObserverem (bez ST), `once`; reduce → statyczny final. Jak IO nie strzeli — stoi finalna: cała klasa błędu znika.
- **2026-07-11 · `main > section` mierzy FANTOMOWY odstęp gdy w `main` są `<div>`** — `gaps.spec` liczył sąsiedztwo po `main > section`, a ProofTicker to `<div>` (nie `<section>`) → odstęp Hero→Problem obejmował 237 px WYPEŁNIONEGO paska tickera i raportował „pas pustki". False positive. Fix: sąsiedztwo po `main > *` (wszystkie bloki treści, nie tylko sekcje); realne pustki wewnątrz sekcji łapie druga pętla (dzieci >200 px). Lekcja: selektor pomiaru layoutu musi objąć KAŻDY bloki-dziecko kontenera, nie wybraną tagem podklasę.
- **2026-07-11 · 44 px hit-area to WCAG AAA, nie prawny próg** — bramka „≥44×44" (2.5.5 AAA) trzyma w czerwieni linki nawigacji i kontrolki w wąskich kartach, których 44 px złamałoby layout. Prawny próg EU 2025 to **AA 2.5.8 = 24×24** z wyjątkami (inline w zdaniu, equivalent, essential). Fix: bramka twarda AA ≥24 (spełniona wszędzie po `min-h-11 md:min-h-0` na przełącznikach/linkach/inputach), AAA ≥44 raportowany jako metryka. Pasmo 24–44 = S/M/L w bento (40 px, grupa full-width) + krótkie linki stopki (inline). Nie zaniżanie bramki — dostrojenie do faktycznego celu zgodności.
- **2026-07-11 · Arena SnapRow = nowy baseline desktop (reduced-motion fallback), NIE regresja** — Comparison ma pin desktop (`motion-reduce:md:hidden`) + gałąź mobilną (`md:hidden motion-reduce:md:block`). Visual biegnie w `reducedMotion:reduce`, więc baseline desktop łapie FALLBACK — po zamianie stosu 4 rund na SnapRow (`md:grid grid-cols-1`) `_roznica` desktop = 100% diff. Realny użytkownik desktop widzi nietknięty pin (motion włączony). Wniosek: „100% diff" w visual-reduced ≠ regresja produkcyjna — sprawdź, którą GAŁĄŹ markупu baseline reprezentuje, zanim uznasz za błąd.
- **2026-07-11 · `VISUAL_BASELINE=1` błogosławi też szum <0,5%** — re-record przepisuje KAŻDY PNG świeżymi pikselami; sekcje w tolerancji (dryf GL, antyaliasing, subpikselowe zmiany po `inline-flex`) git widzi jako zmienione, choć test je przepuszczał. Na desktopie (mobile-only zmiany) to zamazuje dowód „desktop nietknięty". Fix: po re-record ZREWERTOWAĆ baseline desktop poza intencjonalnie zmienionym (`_roznica`) — pozostałe i tak przechodzą <0,5% względem starego baseline. Baseline aktualizować TYLKO gdy diff PRZEKRACZA próg.
- **2026-07-11 · Sufit gęstości: 34,1 → 24,9 ekranu bez usuwania treści** — SnapRow (Problem/Comparison/Pillars/Pricing), bento 2×2, section-pad max-md i typografia zbiły 27%, ale cel ≤22 wymaga jeszcze ~2,9 ekranu = USUNIĘCIA treści (6 kopalni-dem, 4 rundy areny, kalkulator, 8 FAQ) albo ścisku łamiącego premium-whitespace. Gdy dwa twarde ograniczenia (cel liczbowy vs zakaz usuwania treści + jakość) kolidują — nie forsować liczby ściskiem; udokumentować kompromis i oddać decyzję zakresu klientowi.
