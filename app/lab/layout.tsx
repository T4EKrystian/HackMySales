import "./lab.css";

/** LAB — trasy bake-offu (Faza A). Izolowane od produkcji: poza nav/sitemap/robots,
 *  usuwane przed finalnym buildem. Dziedziczy root layout (GLStage/Cursor/grain). */
export default function LabLayout({ children }: { children: React.ReactNode }) {
  return children;
}
